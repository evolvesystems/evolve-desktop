# EvolveApp Desktop - Environment Configuration

# API Configuration (eiq-manager backend)
VITE_API_URL=http://localhost:8547

# Application Configuration
VITE_APP_NAME=EvolveApp
VITE_APP_VERSION=1.0.0
VITE_ENV=development

# Database Architecture
# This desktop app uses REMOTE DATABASE ONLY via API
# No local SQLite - all data stored in eiq-manager PostgreSQL database
# All operations go through the Symfony REST API

# Features
VITE_ENABLE_OFFLINE=false
VITE_ENABLE_NOTIFICATIONS=true

# Optional: API Key for authenticated requests
# VITE_API_KEY=your-api-key-here
